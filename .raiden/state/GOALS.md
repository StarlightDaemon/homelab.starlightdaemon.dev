# Goals

## Current

- Keep the live site stable and CI-passing on all commits.
- Iterate on `network-map/` topology drafts until ready for integration.
- Maintain portability: `verify-standalone.sh` must pass at all times.

## Future

- Integrate approved network map content into the live site when operator authorises.
- Expand homelab documentation as infrastructure evolves.
